name: excelpy
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - xlwings>=0.30.0
  - pandas>=2.0.0
  - numpy>=1.24.0
  - scikit-learn>=1.3.0
  - scipy>=1.10.0
  - openpyxl>=3.1.0
  - pip
  - pip:
    # Optional: Add pip-only packages here if needed

